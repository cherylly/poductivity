version: "3.8"
services:
  app:
    build: .
    ports:
      - "8081:8080"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
    command: python -m src.cli serve
